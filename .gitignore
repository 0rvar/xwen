/target
.direnv
.DS_Store
/models
# The parity oracle is a git submodule pinned via the gitlink (see .gitmodules);
# only its build output is ignored. scripts/build-llamacpp.sh builds it.
reference/llama.cpp/build
.claude/*.lock


# Added by cargo
#
# already existing elements were commented out

#/target
