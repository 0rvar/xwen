# Decisions

This file is the WHY, by topic: every deliberate choice, default, policy, and refuted
direction, with its evidence. Refuted directions are recorded as decisions too — "we
measured it and we will not do X" is load-bearing. Each entry ends with a parenthesized
date pointing at the `docs/log.md` entry that tells the whole story; decisions.md gets
amended in place, the log entry preserves what was believed at the time.

Inherited rule from laguna, kept because it bit that project three separate times: an
observed identity must never be promoted to a claimed one. When this file says
"bit-identical", it says why.

## Topics

One file per topic under `docs/decisions/`; each bullet below is a decision's opening
lead, verbatim, so a name quoted anywhere in the repo (`decisions.md "Serving"`,
`decisions.md "Ceilings"`) greps to its topic file. New decisions go into the topic file
they belong to; a new topic gets a new file and a line here.

- [Scope](decisions/scope.md)
  - xwen is a fork of ../laguna (crate `maxuna`) adapted to Qwen 3.6, serving exactly two…
  - The dependency set is laguna's, verbatim, and is not relitigated.
  - Text-only.
- [Defaults and CLI surface](decisions/defaults-and-cli.md)
  - Default checkpoints are ggml-org's Q4_K_M files.
  - Qwen3.8-27B is a registry entry, not a port (2026-08-14).
  - Sampling defaults follow generation_config.json: temp 1.0, top_p 0.95, top_k 20.
  - Sampling defaults are mode-keyed: thinking temp 1.0 / top_p 0.95, non-thinking 0.7 /…
  - `--reasoning-effort` on a 3.6 checkpoint is a startup error, not a no-op…
  - `max_ctx` is a ceiling, not an allocation; the CLI defaults to 131072 and serve to…
- [Ground truth and parity methodology](decisions/ground-truth-and-parity.md)
  - Upstream llama.cpp master replaces the poolside fork as parity ground truth.
  - The oracle is PINNED to a commit, not tracked.
  - The oracle checkout is a git SUBMODULE
  - Floors are calibrated across BOTH checkpoints and all fixtures, then set under the…
  - Tap-name translation lives in the harness, not in the engine.
  - HF transformers is the secondary reference, with two conversion deltas that make…
- [Model math: the forms that could have gone the other way](decisions/model-math.md)
  - silu runs over the WHOLE fused DeltaNet stream, before the q/k/v split — so q and k…
  - The q/k L2 normalization uses ggml's clamp form `x / max(‖x‖, eps)`, NOT HF's…
  - The `1/√128` scale is applied to the readout only.
  - q and k are broadcast from K-heads up to V-heads by TILING — output head `j` reads…
  - The DeltaNet output norm is `rms_norm → × ssm_norm.weight → × silu(z)`, with the gate…
- [Weights and loading](decisions/weights-and-loading.md)
  - GGUF is the only weight format
  - Loader name traps, recorded here because each one silently produces a working-looking…
  - beta and alpha ship as two separate tensors; there is no fused `ssm_ba` on these…
  - Persistent state is partition-dependent in its low bits, and that is accepted, not…
- [Kernel policy](decisions/kernel-policy.md)
  - Laguna's kernel policy is inherited wholesale:
  - DeltaNet gets a frozen reference first, kernels second.
  - The fused DeltaNet scan is bounded, not bit-identical — so it is pinned OFF in the…
  - A multi-token chunk under an armed rollback checkpoint stays on the reference scan.
  - SUPERSEDED 2026-07-29 (P9a)
  - The three glue kernels ARE bit-identical, and that was worth the block-scope…
  - One kill-switch covers the whole MoE block glue, because all of it is bit-identical.
  - The shared expert is ONE dispatch at decode, and it ships on by default even though it…
  - hc planes are dense_mm-only.
  - Rejected: keeping the hc dense-gemm route off by default until a qwen4exp oracle…
  - The MoE router matmul stays candle's; the fusion starts at the logits.
  - But the projection's DISPATCH is now a vendored gemv at 1..8 rows, and its pin on the…
  - Reproducing candle's arg-sort tie order is not optional, and it is not the stable…
  - The dense-FFN prefill gemm dequantizes in-kernel; it does not dequantize to a…
  - The gap is kernel efficiency, not a memory wall, and the argument needs no peak-…
  - (b) shipped
  - The dense-FFN gemm crosses over at exactly 33 tokens, and that is a tile boundary, not…
  - The dense-FFN gemm is LESS accurate than the `QMatMul` chain it replaces, and that is…
  - The FFN prefill planes cost no device memory, which is the only reason they exist.
  - The small-batch matmul window routes from ONE decision point, and it is…
  - Two deliberate divergences from ggml's own gating, both because our fallback is worse…
  - This kernel is MORE accurate than what it replaces — the opposite of the `dense_mm`…
  - Provenance treatment mirrors `dense_mm`, even though the gate cannot exercise this…
  - The recurrent state is fp32, non-negotiable.
  - The beta|alpha projection folds into its own head at up to 32 tokens — a dispatch remove…
  - It is bounded, not bitwise
  - State the text claim precisely: byte-identical over the window that was GRADED, not…
  - The prefill chunk is per architecture — 2048 on the MoE checkpoints, 512 on the dense…
  - mm_id tiles: the pass-2 grid is a work list, and the token tile is 64 wide when…
  - Dispatch-floor levers ranked (2026-08-30).
- [Refuted perf directions — do not reopen without new evidence](decisions/refuted-perf-directions.md)
  - Refuted: the f32-tile mm_id family (`_t_hp`) as a way to delete the rescale chain…
  - The dual-weight expert gather — fusing gate, up and their SwiGLU into one dispatch —…
  - The DeltaNet scan decomposition: llama.cpp's Metal shape is REFUTED here, and the…
  - A decode-specialized scan kernel is a WASH, and the "60 GB/s scan" that motivated it…
  - "`attn_qkv` runs at a third of its siblings' rate" is REFUTED — at DRAM it is the…
  - The 346 was not reproduced, and the entry does not claim it was.
  - How to read `XWEN_GDN_PROFILE`: it RANKS steps, it does not PRICE them.
  - Chunk-boundary device syncs and command-buffer batching granularity are both REFUTED…
  - Extending the `mul_mv_ext` window past seq 8 is REFUTED — ggml's `ne11_mm_min` 8 is…
  - Refuted: tensor cores for the decode gemv.
  - Refuted: reducing the number of graph splits.
  - Refuted AS ALREADY PRESENT: "adopt MLX-style concurrent encoding".
- [Speculative decoding](decisions/speculative-decoding.md)
  - dflash.rs stays in the fork — a removal decision was made and reversed within the…
  - A DeltaNet layer's spec-decode rollback is a recorded per-token trail, not a…
  - MTP sidecars are a second drafter option, deferred.
  - Drafting is OPT-IN, reversing laguna's opt-out default.
  - opt-in stays
  - The drafter's per-token cache sync is what decides whether speculation pays, not its…
  - `draft_p_min` is PER-CHECKPOINT — 0.5 on the 27B, 0.3 on the 35B-A3B — while…
  - The pause machinery is not free even in a regime where it never pauses, and the two…
  - Speculative decoding's batching win does not currently exist in the DeltaNet layers, and…
  - The drafter's sliding window is implemented as a cache narrow plus a ≤15-column mask,…
  - A drafter is checked against its target in exactly one place, because two places…
  - Three drafter-graph forms came from the oracle, not from the inherited code, and all…
  - Cache sizing figures are derived per checkpoint on `hub::Model`, not carried as…
  - Qwen3.8-27B drafts with its own MTP head, chosen over DSpark, EAGLE3 and transferring…
  - The MTP head's `h` input is the target's POST-final-norm hidden, not a pre-norm layer…
  - The draft chain stays on the GPU; only its final result is read back.
  - `draft_p_min` is a FULL-VOCAB probability in xwen and deliberately not llama.cpp's…
  - A failed drafter reset or import leaves the head untouched, allocating before it…
  - A stored MTP cache image is usable at EXACTLY the position it ends at, where a DFlash…
  - Chain depth is a per-DRAFTER-KIND default, and on the MTP head it is the knob that…
  - The auto-pause controller costs 3-6% on a checkpoint it never pauses, and the shared…
  - The MTP graph is confirmed end-to-end against llama.cpp, by identical text rather than…
- [Serving](decisions/serving.md)
  - The serve/ tree (Anthropic + OpenAI + native dialects, TUI, queue, prefix cache, disk…
  - A mid-conversation system turn demotes to a user turn on the wire dialects…
  - Two cache slots and an opt-in disk tier (2026-08-30; were four and on-by-default).
  - One server serves both checkpoints; `--model` is only the default (2026-08-11).
  - On the wire a checkpoint has exactly one name — its full name — and an unknown one…
  - The GGUF names itself; the architecture is only a fallback (2026-08-14).
  - `--model-size` is a tie-break, not an override (2026-08-14, review round).
  - A job names a FILE, not just a checkpoint (2026-08-14, review round).
  - The disk tier stays bound to the default checkpoint.
  - Speculation is decided per checkpoint, not per process (2026-08-14, review round).
  - `draft.p_min` resolves at drafter attach, not in the config merge (2026-08-11).
  - `/xwen/v1/batch` is the CLI's document over HTTP, run as one queue job…
  - The request-body cap is an explicit 100 MB, replacing axum's implicit 2 MB…
  - One `reasoning_effort` field drives both the think budget and the 3.8 template…
  - `chat_template_kwargs` is validated strictly — the one exception to the compat…
  - Normalization passes assistant reasoning through in native tools mode; retention is…
  - Prefix reuse is quantized to snapshots, so a conversation resumes almost free and an…
- [The prefix cache and the disk tier](decisions/prefix-cache-and-disk-tier.md)
  - `CONTAINER_VERSION` stays at 2 for the DeltaNet snapshot variant — deliberately, not…
- [Batch](decisions/batch.md)
  - `xwen batch` snapshots at the EXACT longest common token, not at a chunk-64…
  - Grouping is single-level, the cache is RAM-only, and it dies with the request.
  - Three floors keep the snapshot honest, and one of them is a correctness precondition…
  - The drafter rides along by truncation, not by a `DrafterImage` export.
  - Batch sampling defaults to greedy and thinking defaults to OFF, and both are…
  - A scored field's value is ASSEMBLED by the engine, not decoded under a grammar mask…
  - An option's score includes the one delimiter token that follows it, which is what…
  - Seam canonicality is CHECKED at plan time, not assumed.
  - `escape` was opener-level mass, confounded by formatting for bare literals —…
  - `escape` is a whole-row classification by token TEXT, with formatting factored out —…
  - `shared_prefix` is a wire-size field, deliberately NOT a prefill feature.
  - Scores are not bit-stable between the cached and cold arms, and consumers must…
- [Metrics](decisions/metrics.md)
  - The history is an append-only JSONL file, not SQLite and not a binary log.
  - It lives in the XDG state directory, not the cache directory.
  - One `write_all` of the whole line, terminator included.
  - `XWEN_METRICS_FILE` is an environment variable, with no `serve.toml` key.
  - Dates are hand-rolled, not a `chrono`/`time` dependency.
  - A bucket's rate is sum(tokens) / sum(seconds), never the mean of its runs' rates.
  - `cached_tokens` is per-surface by construction, and on a batch neither token count is…
  - `ok` is "the run reached its own end", not "the machinery did not throw".
  - A failed or interrupted record carries what was measured, and zero only when nothing…
  - An absent optional means not measured; it never means zero.
  - The metrics label for a one-shot run is the checkpoint's full name, or the GGUF's file…
  - The session id is read from the header and the client id is stored raw.
  - Reading the ids did not make either of them validated.
  - Client-supplied ids are cut to 128 characters.
  - Recording is on by default on every surface, benchmark runs included.
  - Records carry a `v` and readers ignore fields they do not know.
- [Tokenization, chat, tool calls](decisions/tokenization-chat-tool-calls.md)
  - The Qwen tokenizer.json (12,807,982 bytes, byte-identical between the two model…
  - Qwen3.8's tokenizer.json differs from 3.6's by seven added tokens and nothing else,…
  - Qwen3.8 ships a different chat template; it is vendored beside 3.6's and the renderer…
  - The renderer is parameterized by `ChatDialect`, and the 3.8 divergences above are…
  - chat.rs is a hand-written Rust port of the official chat_template.jinja (7764 bytes,…
  - One deliberate divergence from template byte-parity: a tool result as the FIRST…
  - Bodies are stripped with Python's str.strip() whitespace set (29 codepoints,…
  - Constrained decoding's control-token safety is a compile-time property enforced by a…
  - tokenizer.rs is the single owner of every token id in the crate
  - The serve engine parses Qwen's real call format, and a span it cannot read degrades to…
  - `--ban-string` protects the stop ids the decode loop actually uses.
- [Thinking budget and sampling controls](decisions/thinking-and-sampling.md)
  - Top-p renormalizes over the top-k survivors before the cut, following llama.cpp and…
  - A NaN in the logit row fails the draw; ties at the top-k boundary go to the lowest…
  - Ids past the tokenizer's vocabulary are not drawable.
  - The cards' recommended penalties are refused for now, not quietly half-done…
- [Measurement discipline](decisions/measurement-discipline.md)
  - Achievable bandwidth is MEASURED, not quoted, since 2026-09-05: argue bytes-moved…
  - A/B perf comparisons must INTERLEAVE the two arms, and a sequential matrix is not a…
  - This does NOT touch the parity gate, and it is worth saying so explicitly.
  - `pgrep -f` is not a usable "is a model running?" guard — test what the process is…
  - Per-stage forward timing is done IN SITU, by device sync, and only its…
  - The reading discipline is the load-bearing part.
  - Retuning the drafting controller is a scripted protocol now, `scripts/retune-draft.ts`,…
  - A warm-up pass that reads faster than the timed pass following it is a thermal…
  - `--stats` reports drafting against a plain baseline measured INSIDE the same run, and…
- [Qwen3.8-Flash-Next (qwen4exp)](decisions/flash-next.md)
  - Block keys are cached per complete block (2026-08-30).
  - Decode selection runs on the device (2026-08-30).
  - Third arch, composition over forking.
  - The PLE table lives on the CPU side.
  - Refuted: building a cache or eviction policy for the PLE table.
  - Reference-first for every new component.
  - Text-only; MTP deferred; serve after CLI.
  - Weights: Unsloth first, and `UD-Q4_K_XL` specifically.
  - The 640-column rule, and why Q5_1 became unavoidable.
  - One oracle, no vendored copies.
  - The loader owns GGUF tensor-table parsing.
  - QSA rides the existing attention block through an overlay, and decodes by row…
  - QSA pooled keys stay f32; no round-back to the cache dtype.
  - P2 keeps the new recurrent state out of `LayerCache`, and refuses what it cannot…
  - PLE in P2 is host-hybrid, knowingly.
  - PLE decode readbacks share one staging buffer and one wait (2026-09-05).
  - PLE gate and conv run on device for multi-token forwards, the default since 2026-09-05…
  - Refuted: the pre-release architecture priors.
  - Flash-Next is `generate`/`chat`-only until P4, by construction rather than by…
  - Flash-Next is the default checkpoint; serve falls back rather than refuses.
  - AMENDED 2026-08-30 (same day): `xwen batch` is on serve's side of that line, not the…
  - RETIRED 2026-08-30 by the entry below.
  - Two kinds of recurrent state, two routes into a cache image — and the PLE image rides…
  - The FILE identifies the checkpoint on the one-shot path too, not just in serve.
  - Space→hyphen folding applies to the exact-name comparison only.
  - Every Flash-Next-only cost gets its own profiler stage, and profiled decode numbers are…
  - Q5_1 in the vendored `mm_id`, copied verbatim from the pinned llama.cpp.
  - The hyper-connection glue is fused; the two Q8_0 gemms are not.
  - K2 and K4 are bit-identical to the chains they replace and K1 and K3 are bounded
  - Below 32 tokens the hc norm splits across streams, and it is the same arithmetic.
  - At decode the whole read gate is two launches, and it keeps the wide grid (2026-09-05).
  - And the 2..8-token window is where the gate wins biggest: +57-76% (2026-09-06).
  - PLE rows are prefetched, advisorily, and never gated on the gate.
  - never gated on the PLE gate value
  - Refuted: the ~15 GB of private memory is a leak.
  - Forced replay against llama.cpp is Flash-Next's math gate while the harness panics, and…
  - Phased, correctness before speed.
  - Ceilings, as of 2026-09-05 (log.md "Ceiling diagnosis"), and how the ledger reads…
  - In decode mode that caveat is the rule, not the corner case (2026-09-06,…
  - And the launch budget itself is refined, 2026-09-06 (log.md "Fused MoE shared expert";…
  - The rule, for ranking every remaining fusion candidate: (launches removed × ~4 µs) is…
  - And a third class of decode cost, named 2026-09-06 (log.md "Router projection on a…
- [Process](decisions/process.md)
